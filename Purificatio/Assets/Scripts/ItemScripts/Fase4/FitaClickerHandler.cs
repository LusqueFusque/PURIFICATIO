using UnityEngine;
using UnityEngine.EventSystems;
using UnityEngine.UI;

public class FitaClickHandler : MonoBehaviour
{
    // Não precisa de Setters, pois o FitaItem é um singleton
    
  //  public void OnPointerClick(PointerEventData eventData)
  //  {
   //     var fitaItem = FitaItem.Instance; 
    //    bool fitaAtiva = fitaItem != null && fitaItem.IsActive();
//
    //    if (fitaAtiva)
      //  {
      //      Debug.Log("[FitaClick] Interação bem-sucedida!");
      //      fitaItem.UseFitaOnImage(); 
      //  }
     //   else
      //  {
      //      Debug.Log("[FitaClick] Fita não está ativa — bloqueado.");
     //   }
   // }
}